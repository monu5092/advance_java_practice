server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/monu5092/advance_java_practice.git
          default-label: main
          clone-on-start: true 
          search-paths: config
          