server:
  port: 8888


spring:
  cloud:
    config:
      server:
          git:
            uri: https://github.com/monu5092/microservices.git
            default-label: main
            clone-on-start: true 
            search-paths: config